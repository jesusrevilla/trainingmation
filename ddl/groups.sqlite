CREATE TABLE GROUPS(
  ID INTEGER PRIMARY KEY,
  GROUP_NAME TEXT NOT NULL,
  STUDENT_NAME TEXT NOT NULL,
  STUDENT_ID_NUM INTEGER NOT NULL UNIQUE,
  STUDENT_ID_TXT NOT NULL UNIQUE,
  IS_ACTIVE INTEGER NOT NULL
  );
